<%= link_to "#{@post.category.name}", show_category_path(@post.category_id) %>
<h1><%= @post.title %></h1>
<p><%= @post.description %></p>
<h6><%= @post.email %></h6>
<p>$<%= @post.price %></p>
<%= link_to 'Delete Post', destroy_post_path,
			 method: :delete, data: { confirm: 'Are you sure?' } %> 
